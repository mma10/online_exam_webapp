const initState = {
    id: null,
    type: null,
    loggedIn: null,
    name: null,
}

const authReducer = (state = initState,action) => {
    switch(action.type){
        default: return state;
    }

}

export default authReducer;